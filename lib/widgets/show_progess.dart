import 'package:flutter/material.dart';

class ShowProgess extends StatelessWidget {
  const ShowProgess({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: CircularProgressIndicator(),
    );
  }
}
